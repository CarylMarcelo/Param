# Param application

This directory contains the PHP application served by XAMPP. The repository-level `README.md` documents setup and architecture.

Do not copy or commit `src/config/mail.local.php`; it contains machine-local SMTP configuration.
